<% if obj.errors.any? %>
    <div class="alert alert-danger">
        <h2> <%= pluralize(obj.errors.count, "error") %> prevented the article from getting saved:</h2>
        <ul>
            <% obj.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        </ul>
    </div>
<% end %>
